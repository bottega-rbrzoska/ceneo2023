import { TestStrDirective } from './test-str.directive';

describe('TestStrDirective', () => {
  it('should create an instance', () => {
    const directive = new TestStrDirective();
    expect(directive).toBeTruthy();
  });
});
